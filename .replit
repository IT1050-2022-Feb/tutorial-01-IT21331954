language = "bash"
run = "chmod +x run.sh;./run.sh"
#include<stdio.h>
int main ();
{
int marks1,marks2;
float avg;
printf("input marks 1 :");
scanf("%d", &marks1);
printf("Input marks 2 :");
scanf("%d", &marks2);
avg=(marks1 + marks2)/2;
printif("Average is :");
scanf("%d", &avg);
return 0;
}